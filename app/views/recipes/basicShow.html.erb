<div class="recipe">
	
	<div class="row row-centered">
		
		<div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
			<div id="recipeImage" class="img-responsive img_wrapper">
				<% if @recipe.image.present? %>
					<%= image_tag @recipe.image.url(:medium) , :class => "img-responsive img-center center-block" %>
				<% end %>
		
			</div>
		</div>

		

		<div class="col-lg-12 col-md-12 col-sm-12 col-xs-12 col-centered">
			<h1><%= @recipe.title %></h1>
			<p><%= @recipe.description %></p>
			<p><%= @recipe.user.email %></p>
		</div>

		<div class="col-lg-12 col-md-12 col-sm-12 col-xs-12 col-centered">
			<% if @recipe.ingredients.present? %>
				<h5><strong>Ingredients</strong></h5>
				<% @recipe.ingredients.each_with_index do |ingredient,index| %>
					<p><%= (index+1).to_s + ".)" + " " + ingredient.name %>
					</p>
				<% end %>
			<% end %>
		</div>

		<div class="col-lg-12 col-md-12 col-sm-12 col-xs-12 col-centered">
			<% if @recipe.directions.present? %>
				<h5><strong>Directions</strong></h5>
				<% @recipe.directions.each_with_index do |direction,index| %>
					<p><%= (index+1).to_s + ".)" + " " + direction.step %>
					</p>
				<% end %>
			<% end %>
				
		</div>

		<div class="col-lg-12 col-md-12 col-sm-12 col-xs-12 col-centered">
			<%= link_to "Back" , root_path , class: "btn btn-default" %>
		</div>

	</div>
	
</div>


